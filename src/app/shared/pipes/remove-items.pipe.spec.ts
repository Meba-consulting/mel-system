import { RemoveItemsPipe } from './remove-items.pipe';

describe('RemoveItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
