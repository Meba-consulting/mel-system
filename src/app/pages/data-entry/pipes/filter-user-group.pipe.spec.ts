import { FilterUserGroupPipe } from './filter-user-group.pipe';

describe('FilterUserGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterUserGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
