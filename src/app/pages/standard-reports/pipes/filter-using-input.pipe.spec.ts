import { FilterUsingInputPipe } from './filter-using-input.pipe';

describe('FilterUsingInputPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterUsingInputPipe();
    expect(pipe).toBeTruthy();
  });
});
